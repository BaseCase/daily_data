document.addEventListener("DOMContentLoaded", function() {
  var width = 640,
      height = 480,
      chart = d3.select('#chart').append('svg')
                .attr('width', width)
                .attr('height', height);
});
